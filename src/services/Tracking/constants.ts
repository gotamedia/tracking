export enum TrackingEventNames {
    ActiveUsage = 'active usage',
    FirstVisit = 'first visit',
    SessionStart = 'session start',
    PageView = 'page view',
    Video = 'video content start',
    PaywallLoaded = 'paywall loaded',
    EcommerceReceipt = 'ecommerce receipt'
}

export enum TrackingEventContentTypes {
    LongRead = "long read",
    RealEstate = "real estate",
    Native = "native",
    Article = "article",
    Start = "start",
    NewsFeed = "news feed"
}

export enum VideoTypes {
    Milestone = 'milestone'
}

export enum GtmEventNames {
    BnTracking = 'BN_TRACKING'
}